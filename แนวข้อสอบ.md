# แนวข้อสอบ เตรียมความพร้อม

เอกสารนี้นำสาระจากข้อสอบก่อนเรียนทั้ง 50 ข้อมาร้อยเรียงเป็นบทอ่านสั้น ๆ เพื่อให้ผู้เรียนเห็นภาพรวมก่อนเริ่มเรียนจริง เนื้อหาเริ่มจากพื้นฐานเว็บและการเขียนโปรแกรม แล้วค่อยเชื่อมไปยัง React, API, Git, Testing และการควบคุม SmartLamp ผ่าน Bluetooth Low Energy (BLE)

เป้าหมายไม่ใช่การจำคำตอบเป็นข้อ ๆ แต่คือการเข้าใจว่าแต่ละแนวคิดเชื่อมต่อกันอย่างไร เมื่ออ่านจบควรสามารถอธิบายคำศัพท์สำคัญ เลือกเครื่องมือให้ถูกหน้าที่ และวิเคราะห์สถานการณ์พื้นฐานของแอป SmartLamp ได้

---

## 1. ภาพรวมของ Front-End และการทำงานของเว็บ

Front-End คือส่วนของซอฟต์แวร์ที่ผู้ใช้มองเห็นและโต้ตอบโดยตรง เช่น หน้าเข้าสู่ระบบ เมนู ปุ่ม รูปภาพ แบบฟอร์ม หรือหน้าควบคุมหลอดไฟ SmartLamp งาน Front-End จึงไม่ได้มีเพียงการทำให้หน้าจอสวย แต่ต้องทำให้ข้อมูลถูกต้อง ใช้งานง่าย รองรับหลายขนาดหน้าจอ และตอบสนองต่อการกระทำของผู้ใช้ด้วย

ผู้ใช้เปิดเว็บไซต์ผ่าน **Web Browser** เช่น Chrome, Edge, Firefox หรือ Safari หน้าที่หลักของ Browser คือรับไฟล์และข้อมูลจากระบบ แล้วแปลผลออกมาเป็นหน้าเว็บที่ผู้ใช้มองเห็น เมื่อเราพิมพ์ที่อยู่เว็บไซต์ สิ่งที่พิมพ์เรียกว่า **URL (Uniform Resource Locator)** ซึ่งใช้ระบุตำแหน่งของหน้าเว็บหรือทรัพยากรบนเครือข่าย

หน้าเว็บพื้นฐานเกิดจากเทคโนโลยีหลักสามส่วน ได้แก่ HTML, CSS และ JavaScript แต่ละส่วนมีหน้าที่ต่างกันอย่างชัดเจน

- HTML สร้างโครงสร้างและความหมายของเนื้อหา
- CSS กำหนดรูปลักษณ์ สี ระยะห่าง และการจัดวาง
- JavaScript เพิ่มพฤติกรรมและการโต้ตอบ

ให้นึกถึงบ้านหนึ่งหลัง: HTML คือโครงสร้างห้องและประตู, CSS คือสีและการตกแต่ง ส่วน JavaScript คือระบบที่ทำให้สวิตช์ ประตู หรืออุปกรณ์ต่าง ๆ ตอบสนองเมื่อมีคนใช้งาน

> **โน้ต:** Browser ไม่ใช่ฐานข้อมูลหรือ Compiler โดยตรง หน้าที่หลักของ Browser คือเปิด แปลผล และแสดงทรัพยากรเว็บให้ผู้ใช้ใช้งานได้

---

## 2. HTML: โครงสร้างและความหมายของหน้าเว็บ

**HTML** ย่อมาจาก **HyperText Markup Language** เป็นภาษามาร์กอัปสำหรับกำหนดโครงสร้างของเอกสารเว็บ ไฟล์ HTML โดยทั่วไปใช้นามสกุล `.html` ภายในไฟล์ประกอบด้วย element หรือแท็กที่บอกว่าเนื้อหาแต่ละส่วนมีหน้าที่อะไร

แท็กพื้นฐานที่ควรรู้ก่อนเริ่มเรียนมีดังนี้

```html
<a href="https://example.com">เปิดเว็บไซต์</a>
<img src="lamp.png" alt="หลอดไฟอัจฉริยะ">
<label for="lamp-name">ชื่อหลอดไฟ</label>
<input id="lamp-name" type="text">
```

แท็ก `<a>` ใช้สร้างลิงก์ไปยังหน้าอื่นหรือทรัพยากรอื่น ส่วน `<img>` ใช้แสดงรูปภาพ โดยข้อความใน `alt` ช่วยอธิบายรูปให้ผู้ใช้ที่มองไม่เห็นภาพหรือเมื่อรูปโหลดไม่สำเร็จ แท็ก `<input>` ใช้รับข้อมูล เช่น ชื่อ อีเมล รหัสผ่าน หรือค่าที่ผู้ใช้กรอก

HTML ที่ดีควรสื่อความหมายของส่วนต่าง ๆ ไม่ใช่ใช้แท็กทั่วไปกับทุกอย่าง ตัวอย่างเช่น `<nav>` เหมาะสำหรับบริเวณเมนูนำทาง เพราะชื่อแท็กบอกหน้าที่ของเนื้อหาอย่างชัดเจน แนวทางนี้เรียกว่า **Semantic HTML** และช่วยทั้งผู้พัฒนา Search Engine และเทคโนโลยีช่วยเหลือผู้พิการ

การทำแบบฟอร์มที่เข้าถึงได้ควรเชื่อม `<label>` กับ `<input>` โดยใช้ค่า `for` ของ label ให้ตรงกับ `id` ของ input วิธีนี้ทำให้ผู้ใช้โปรแกรมอ่านหน้าจอทราบว่าช่องนั้นต้องกรอกอะไร และผู้ใช้ยังสามารถกดที่ข้อความ label เพื่อเลือกช่องกรอกได้ด้วย

> **จำให้แม่น:** Placeholder เป็นเพียงข้อความตัวอย่างภายในช่องกรอก ไม่ควรใช้แทน label ทั้งหมด เพราะข้อความอาจหายไปเมื่อเริ่มพิมพ์และอาจไม่ชัดเจนสำหรับผู้ใช้บางกลุ่ม

---

## 3. CSS: รูปลักษณ์ การจัดวาง และหน้าจอหลายขนาด

**CSS (Cascading Style Sheets)** ใช้กำหนดหน้าตาของ HTML เช่น สีตัวอักษร สีพื้นหลัง ขนาด ระยะห่าง เส้นขอบ และตำแหน่งของ element CSS ไม่ได้ทำหน้าที่เก็บข้อมูลในฐานข้อมูลหรือเชื่อมต่อ Bluetooth แต่ทำให้โครงสร้าง HTML กลายเป็น UI ที่อ่านและใช้งานได้ง่าย

CSS เลือก element ผ่าน **selector** หาก element มี `id="menu"` จะเลือกด้วย `#menu` ดังตัวอย่าง

```css
#menu {
  display: flex;
  gap: 12px;
}
```

เครื่องหมาย `#` ใช้เลือก id ส่วนเครื่องหมาย `.` ใช้เลือก class จึงต้องแยกให้ถูก เช่น `#menu` หมายถึง id ชื่อ menu ขณะที่ `.menu` หมายถึง class ชื่อ menu

การจัดวางสมัยใหม่มักใช้ **Flexbox** และ **CSS Grid** Flexbox เหมาะกับการจัดวางแบบหนึ่งมิติ เช่น เรียงปุ่มเป็นแถวหรือเรียงเมนูเป็นคอลัมน์ ส่วน Grid เหมาะกับการจัดวางแบบสองมิติที่ต้องควบคุมทั้งแถวและคอลัมน์ เช่น แผงการ์ดอุปกรณ์หลายใบ

เว็บไซต์และแอปต้องทำงานบนหน้าจอหลายขนาด แนวคิด **Responsive Design** คือการออกแบบให้เนื้อหาและการจัดวางปรับตามพื้นที่ของอุปกรณ์ ไม่ว่าจะเป็นโทรศัพท์ แท็บเล็ต หรือคอมพิวเตอร์ เป้าหมายไม่ใช่ทำให้ทุกหน้าจอเหมือนกันทุกพิกเซล แต่ทำให้ทุกหน้าจออ่านง่าย กดง่าย และใช้งานได้ครบ

> **แนวคิดสำคัญ:** เลือก Flexbox เมื่อสนใจทิศทางหลักเพียงแกนเดียว เลือก Grid เมื่อความสัมพันธ์ระหว่างแถวและคอลัมน์มีความสำคัญ

---

## 4. JavaScript: ข้อมูล คำสั่ง และการโต้ตอบ

JavaScript ทำให้หน้าเว็บหรือ UI ตอบสนองต่อผู้ใช้ได้ เช่น กดปุ่มแล้วเปิดไฟ เลื่อน slider แล้วเปลี่ยนความสว่าง เลือกสีแล้วอัปเดตค่าบนหน้าจอ หรือเรียกข้อมูลจากระบบอื่น

ข้อมูลในโปรแกรมมักถูกเก็บไว้ใน **ตัวแปร (variable)** เพื่อให้เรียกใช้และประมวลผลภายหลัง ตัวอย่างเช่น

```js
const lampName = "SmartLamp";
let brightness = 50;
const isConnected = true;
const favoriteColors = ["#FF0000", "#00FF00", "#0000FF"];
```

`lampName` เก็บข้อความ, `brightness` เก็บตัวเลข, `isConnected` เป็น **Boolean** ซึ่งมีค่าพื้นฐานเพียง `true` หรือ `false` และ `favoriteColors` เป็น **Array** สำหรับเก็บข้อมูลหลายรายการตามลำดับ

`const` ใช้กับตัวแปรที่ไม่ต้องการให้เปลี่ยนการอ้างอิงหลังประกาศ ดังนั้นเมื่อเขียน `const score = 10` แล้ว จะไม่สามารถเขียน `score = 20` ได้ หากค่าต้องเปลี่ยนควรพิจารณาใช้ `let` อย่างไรก็ตาม object หรือ array ที่ประกาศด้วย `const` ยังสามารถเปลี่ยนข้อมูลภายในได้ เพียงแต่ไม่สามารถนำตัวแปรนั้นไปอ้างอิง object หรือ array คนละตัวโดยตรง

**Function** คือชุดคำสั่งที่ตั้งชื่อและเรียกใช้ซ้ำได้ ช่วยลดการเขียนโค้ดซ้ำและแยกหน้าที่ให้ชัดเจน

```js
function percentToLevel(percent) {
  return Math.round((percent / 100) * 255);
}
```

เมื่อมีข้อมูลเป็น Array เราสามารถใช้ `map()` เพื่อแปลงสมาชิกทุกตัวและรับ Array ใหม่กลับมา

```js
const levels = [0, 50, 100].map(percentToLevel);
// ผลลัพธ์: [0, 128, 255]
```

จุดสำคัญคือ `map()` ใช้เมื่อต้องการผลลัพธ์ใหม่จากสมาชิกทุกตัว ไม่ใช่ใช้เพิ่มหรือลบสมาชิกท้าย Array แบบ `push()` หรือ `pop()`

---

## 5. DOM และ Event: จากโครงสร้างสู่การกระทำ

เมื่อ Browser โหลด HTML จะสร้างแบบจำลองที่เรียกว่า **DOM (Document Object Model)** DOM มองเอกสารเป็นโครงสร้างของ object ที่ JavaScript สามารถค้นหา อ่าน และแก้ไขได้ เช่น เปลี่ยนข้อความ ซ่อน element เพิ่มรายการ หรืออัปเดตสถานะปุ่ม

การโต้ตอบของผู้ใช้ถูกส่งมาในรูป **Event** เช่น `click`, `input`, `change` หรือ `submit` เหตุการณ์ `click` เกิดเมื่อผู้ใช้กดเมาส์หรือแตะ element บนหน้าจอ โปรแกรมสามารถลงทะเบียนตัวจัดการเหตุการณ์เพื่อกำหนดสิ่งที่จะเกิดขึ้นต่อไป

```js
powerButton.addEventListener("click", () => {
  statusText.textContent = "กำลังส่งคำสั่งเปิดไฟ";
});
```

ลำดับความคิดพื้นฐานคือ ผู้ใช้กระทำบางอย่าง → Event เกิดขึ้น → Function ทำงาน → ข้อมูลหรือ DOM เปลี่ยน → ผู้ใช้เห็นผลลัพธ์ใหม่

---

## 6. งาน Asynchronous และ TypeScript

บางงานใช้เวลาและไม่ควรหยุดการทำงานทั้งหมดของโปรแกรม เช่น การเรียก API การอ่านไฟล์ หรือการรอคำตอบจากอุปกรณ์ งานลักษณะนี้เรียกว่า **Asynchronous**

`async/await` ช่วยให้เขียนลำดับงาน asynchronous ได้อ่านง่ายคล้ายคำสั่งที่ทำต่อกัน แต่การ `await` จะพักเฉพาะลำดับในฟังก์ชัน async ที่เกี่ยวข้อง ไม่ได้หมายถึงการหยุดโปรแกรมหรือหน้าจอทั้งหมด

```ts
async function loadLampState() {
  const response = await fetch("/api/lamp-state");
  return response.json();
}
```

งานที่อาจล้มเหลวควรมีการจัดการ error เสมอ เพราะเครือข่ายอาจหลุด อุปกรณ์อาจไม่ตอบ หรือข้อมูลอาจไม่ถูกต้อง

**TypeScript** คือ JavaScript ที่เพิ่มระบบชนิดข้อมูล ช่วยตรวจข้อผิดพลาดบางประเภทก่อนรันจริง เช่น ส่งข้อความไปยังฟังก์ชันที่ต้องการตัวเลข หรืออ่าน property ที่ไม่มีอยู่ การมี type ที่ชัดเจนช่วยให้เครื่องมือแก้ไขโค้ดแนะนำได้ดีขึ้น และช่วยให้ทีมเข้าใจข้อตกลงของข้อมูลตรงกัน

```ts
type LampState = {
  power: boolean;
  brightness: number;
  rgb: [number, number, number];
};
```

> **โน้ต:** TypeScript ช่วยลดข้อผิดพลาด แต่ไม่สามารถแทนการตรวจข้อมูลจริง การจัดการ error และการทดสอบซอฟต์แวร์ได้

---

## 7. React: Component, Props และ State

React แบ่ง UI ออกเป็นส่วนย่อยที่เรียกว่า **Component** แต่ละ Component ควรมีหน้าที่ชัดเจนและสามารถนำกลับมาใช้ซ้ำได้ เช่น `PowerButton`, `BrightnessSlider`, `ColorPicker` หรือ `ConnectionBadge`

ข้อมูลที่ส่งเข้าสู่ Component เรียกว่า **Props** โดยทั่วไป Parent Component เป็นผู้ส่งข้อมูลให้ Child Component Props เหมาะกับข้อมูลที่ผู้เรียกเป็นผู้กำหนด เช่น ชื่อหลอดไฟ สี หรือฟังก์ชันที่จะทำงานเมื่อผู้ใช้กดปุ่ม

ข้อมูลที่เปลี่ยนภายใน UI และมีผลต่อสิ่งที่แสดงเรียกว่า **State** ตัวอย่างเช่น สถานะเปิด–ปิด ค่าความสว่าง หรือสถานะกำลังเชื่อมต่อ เมื่อ State เปลี่ยน React จะคำนวณและแสดง UI ใหม่ตามข้อมูลล่าสุด

ใน Function Component มักใช้ Hook ชื่อ `useState` เพื่อจัดการ State

```tsx
const [brightness, setBrightness] = useState(50);

return (
  <BrightnessSlider
    value={brightness}
    onChange={setBrightness}
  />
);
```

ในตัวอย่างนี้ `brightness` คือ State ส่วน `value` และ `onChange` เป็น Props ที่ส่งไปยัง `BrightnessSlider`

> **สรุปสั้น:** Component คือชิ้นส่วน UI, Props คือข้อมูลที่รับเข้ามา และ State คือข้อมูลที่เปลี่ยนแล้วทำให้ UI เปลี่ยนตาม

---

## 8. API, JSON และ HTTP

**API (Application Programming Interface)** คือช่องทางหรือข้อตกลงที่ทำให้ซอฟต์แวร์สองส่วนสื่อสารกันได้ ฝั่งที่ขอข้อมูลส่ง **Request** และอีกฝั่งส่ง **Response** กลับมา ข้อมูลที่แลกเปลี่ยนกันบนเว็บมักใช้รูปแบบ **JSON (JavaScript Object Notation)** เพราะเป็นข้อความที่อ่านง่ายและแปลงเป็น object ได้สะดวก

```json
{
  "power": true,
  "brightness": 80,
  "color": "#AF52DE"
}
```

HTTP มี method เพื่อบอกเจตนาของ Request โดย `GET` ใช้ขออ่านข้อมูลเป็นหลัก ส่วนแนวคิด **CRUD** สรุปการจัดการข้อมูลพื้นฐานสี่แบบ ได้แก่

- Create: สร้างข้อมูล
- Read: อ่านข้อมูล
- Update: แก้ไขข้อมูล
- Delete: ลบข้อมูล

Response ของ HTTP มี **Status Code** เพื่อบอกผลลัพธ์ ตัวอย่างสำคัญคือ `404 Not Found` ซึ่งหมายถึงไม่พบทรัพยากรที่ร้องขอ จึงควรแยกให้ออกจากสถานะสำเร็จอย่าง `200 OK`

> **วิธีคิด:** API คือข้อตกลงการสื่อสาร, HTTP คือกติกาการรับส่งบนเว็บ และ JSON คือรูปแบบข้อมูลที่นิยมส่งอยู่ภายใน Request หรือ Response

---

## 9. Authentication, Authorization และการเก็บ Secret

ระบบที่มีผู้ใช้ต้องแยกสองแนวคิดออกจากกัน

**Authentication** คือการตรวจว่า “ผู้ใช้เป็นใคร” เช่น เข้าสู่ระบบด้วยรหัสผ่านหรือยืนยัน PIN ส่วน **Authorization** คือการตรวจว่า “ผู้ใช้นั้นมีสิทธิ์ทำอะไร” เช่น ผู้ใช้ทั่วไปควบคุมหลอดไฟได้ แต่ไม่มีสิทธิ์เปลี่ยนค่าระบบของผู้ดูแล

ข้อมูลลับ เช่น Password, Secret Key หรือ Server API Key ไม่ควรเขียนค้างไว้ในโค้ดฝั่งผู้ใช้ และไม่ควร Commit ลง GitHub สาธารณะ Secret ฝั่งเซิร์ฟเวอร์ควรเก็บใน Environment Variable ของสภาพแวดล้อมที่ปลอดภัย พร้อมตั้งค่าไม่ให้ไฟล์ลับถูกติดตามด้วย Git

ต้องเข้าใจด้วยว่า Environment Variable ที่ถูกส่งไปรวมในแอปฝั่งผู้ใช้ไม่ถือว่าเป็นความลับ เพราะผู้ใช้สามารถตรวจไฟล์แอปหรือข้อมูลเครือข่ายได้ การตัดสินใจว่า secret ควรอยู่ที่ใดจึงต้องพิจารณาขอบเขตฝั่ง Client และ Server ให้ชัดเจน

---

## 10. Git, Repository และการทดสอบ

**Git** คือระบบควบคุมเวอร์ชัน ใช้ติดตามการเปลี่ยนแปลงของไฟล์และโค้ด ช่วยให้ย้อนดูประวัติ ทดลองทำงานบน branch และทำงานร่วมกับผู้อื่นได้

**Repository** คือพื้นที่ของโปรเจกต์ที่ Git ติดตาม พร้อมข้อมูลประวัติการเปลี่ยนแปลง Repository จึงไม่ใช่แค่โฟลเดอร์เก็บไฟล์ธรรมดา แต่มีข้อมูลว่ามีการแก้อะไร เมื่อใด และบันทึกไว้ใน commit ใด

การทดสอบช่วยยืนยันว่าระบบทำงานตามที่คาดและลดความเสี่ยงเมื่อแก้ไขโค้ด การทดสอบแต่ละระดับมีเป้าหมายต่างกัน

**Unit Test** ตรวจส่วนย่อยขนาดเล็ก เช่น Function แปลง HEX เป็น RGB หรือตรวจ checksum ส่วน **End-to-End Test (E2E)** ตรวจเส้นทางการใช้งานทั้งระบบ เช่น เปิดแอป → เชื่อมต่อหลอดไฟ → ปรับความสว่าง → ตรวจว่าหลอดไฟและหน้าจอแสดงสถานะตรงกัน

Unit Test มักรันเร็วและหาตำแหน่งปัญหาได้ง่าย ส่วน E2E ให้ความมั่นใจในภาพรวมมากกว่าแต่ตั้งค่าและรันได้ซับซ้อนกว่า ระบบที่ดีจึงใช้การทดสอบหลายระดับร่วมกัน ไม่เลือกเพียงแบบใดแบบหนึ่ง

---

## 11. สี RGB และ HEX ใน SmartLamp

SmartLamp ใช้หลอดไฟที่ควบคุมสีด้วยระบบ **RGB** ซึ่งประกอบด้วย Red, Green และ Blue การผสมค่าความเข้มของแม่สีทั้งสามทำให้เกิดสีต่าง ๆ เช่น RGB `(255, 0, 0)` คือสีแดง และ RGB `(255, 255, 255)` คือสีขาว

บนหน้าจอเว็บหรือแอป สีมักเขียนเป็นรหัส **HEX** ที่ขึ้นต้นด้วย `#` เช่น `#FF0000` คือสีแดง และ `#00FF00` คือสีเขียว รูปแบบหกหลักแบ่งเป็นคู่ของค่า Red, Green และ Blue ตามลำดับ

แอป SmartLamp จึงต้องสามารถแสดงและแปลงสีระหว่าง RGB กับ HEX ให้สอดคล้องกัน หากผู้ใช้เลือกสีจากวงล้อสี ค่าบนหน้าจอ คำสั่งที่ส่ง และสถานะที่อุปกรณ์ตอบกลับควรแทนสีเดียวกัน

---

## 12. Bluetooth และ BLE

**Bluetooth** เป็นเทคโนโลยีสื่อสารไร้สายระยะใกล้สำหรับเชื่อมต่ออุปกรณ์ เช่น โทรศัพท์ หูฟัง เซนเซอร์ หรือหลอดไฟอัจฉริยะ

**BLE** ย่อมาจาก **Bluetooth Low Energy** ออกแบบมาให้ใช้พลังงานต่ำและเหมาะกับการส่งข้อมูลขนาดเล็กเป็นช่วง ๆ เช่น คำสั่งเปิด–ปิด ค่าความสว่าง หรือค่าสี SmartLamp ใช้ BLE เป็นช่องทางให้แอปบนโทรศัพท์สื่อสารกับ ESP32 โดยตรง

การเชื่อมต่อไร้สายไม่เสถียรตลอดเวลา ผู้ใช้อาจปิด Bluetooth เดินออกนอกระยะ หรืออุปกรณ์อาจไม่ตอบ แอปจึงต้องแสดงสถานะอย่างตรงไปตรงมา เช่น กำลังค้นหา กำลังเชื่อมต่อ เชื่อมต่อแล้ว หรือเกิดข้อผิดพลาด

---

## 13. การควบคุม SmartLamp อย่างถูกต้องและลื่นไหล

ในช่วงที่แอปเชื่อมต่อกับ ESP32 แล้ว **สถานะที่ ESP32 ยืนยันกลับมาเป็น Source of Truth** หรือค่าจริงหลักของหลอดไฟ หน้าจออาจแสดงผลล่วงหน้าเพื่อให้รู้สึกรวดเร็ว แต่สุดท้ายต้องปรับให้ตรงกับค่าที่อุปกรณ์ตอบกลับ

ตัวอย่างเช่น ผู้ใช้กดเปิดไฟ แอปอาจเปลี่ยนภาพปุ่มทันที แต่ยังไม่ควรถือว่าคำสั่งสำเร็จถาวรจนได้รับการยืนยัน หากอุปกรณ์ตอบว่าคำสั่งล้มเหลวหรือการเชื่อมต่อหลุด UI ต้องแจ้งข้อผิดพลาดและคืนค่าให้ตรงกับสถานะจริง

Slider ความสว่างสร้างค่าใหม่จำนวนมากขณะผู้ใช้ลาก หากส่งทุกค่าทันที ช่องทาง BLE อาจมีคำสั่งสะสมจนตอบสนองช้า จึงควรใช้การจำกัดอัตรา เช่น **Throttle** เพื่อส่งค่าตามช่วงเวลาที่เหมาะสม และต้องส่งค่าสุดท้ายเสมอเพื่อให้หลอดไฟหยุดตรงตำแหน่งที่ผู้ใช้เลือก

> **ตัวอย่าง:** ผู้ใช้อาจลากผ่าน 40 ค่าในหนึ่งวินาที แต่ระบบเลือกส่งประมาณ 10–20 ครั้งต่อวินาที พร้อมยืนยันว่าค่าสุดท้ายถูกส่งหลังหยุดลาก

---

## 14. Revision: ป้องกันสถานะเก่าทับสถานะใหม่

Packet ในระบบไร้สายอาจมาถึงไม่ตรงลำดับเวลา SmartLamp จึงกำกับ State ด้วยหมายเลข **Revision** ซึ่งเพิ่มขึ้นเมื่ออุปกรณ์ยอมรับการเปลี่ยนสถานะ

สมมติว่าแอปได้รับ Revision 21 แล้ว ต่อมาได้รับ Revision 20 ที่เดินทางมาช้า แอปต้องเพิกเฉย Revision 20 เพราะเป็นข้อมูลเก่า หากนำมาแสดงจะทำให้ UI ย้อนกลับไปยังสถานะก่อนหน้า

หลักการเปรียบเทียบคือ

```text
revision ใหม่กว่า revision ล่าสุด  → รับและอัปเดต
revision เท่ากับ revision ล่าสุด   → อาจเป็นข้อมูลซ้ำ ไม่ต้องย้อนสถานะ
revision เก่ากว่า revision ล่าสุด  → เพิกเฉย
```

Revision จึงไม่ได้บอกเวลาเป็นวินาที แต่บอกลำดับการเปลี่ยนแปลงของ State ที่อุปกรณ์ยอมรับ

---

## 15. Sequence และการจัดการคำสั่งซ้ำ

เมื่อแอปส่งคำสั่ง จะกำกับด้วยหมายเลข **Sequence** เพื่อจับคู่คำสั่งกับคำตอบ หาก ACK มาช้า แอปอาจคิดว่าคำสั่งหายและส่งซ้ำ

Firmware ต้องตรวจว่าคำสั่งที่มี Sequence เดิมเป็นคำสั่งซ้ำหรือไม่ หากเป็นคำสั่งซ้ำที่เคยประมวลผลแล้ว ควรตอบ ACK และ State ที่เก็บไว้ใน Cache โดยไม่เปลี่ยน State ซ้ำ วิธีนี้ช่วยให้ Retry ปลอดภัย

ตัวอย่างเช่น คำสั่ง “เพิ่มความสว่าง 10” หากถูกทำซ้ำโดยไม่ตรวจ Sequence อาจเพิ่มความสว่างสองครั้งทั้งที่ผู้ใช้สั่งครั้งเดียว การตรวจคำสั่งซ้ำจึงสำคัญกว่าการตรวจเฉพาะว่าข้อมูลอ่านได้หรือไม่

> **จำแนกบทบาท:** Sequence ใช้ติดตามลำดับคำสั่งและคำตอบ ส่วน Revision ใช้ติดตามลำดับของสถานะที่ถูกยอมรับแล้ว

---

## 16. CRC: ตรวจความเสียหายของ Packet

**CRC (Cyclic Redundancy Check)** เป็นค่าที่คำนวณจากข้อมูลใน Packet ฝั่งรับคำนวณซ้ำแล้วเปรียบเทียบกับค่าที่ส่งมา หากไม่ตรงกัน แสดงว่าข้อมูลอาจเสียหายหรือเปลี่ยนไประหว่างส่ง ระบบควรทิ้ง Packet นั้นและไม่เปลี่ยน State

CRC มีหน้าที่ตรวจความสมบูรณ์ของข้อมูล แต่ **ไม่ใช่การเข้ารหัส** และ **ไม่ใช่การยืนยันตัวตน** ผู้โจมตีที่รู้วิธีคำนวณ CRC สามารถสร้างค่าใหม่ให้ข้อมูลที่แก้ไขได้ ดังนั้นระบบที่ต้องการความปลอดภัยยังต้องมี Authentication และกลไกป้องกันอื่นร่วมด้วย

---

## 17. แยก UI ออกจาก BLE ด้วย Repository และ Use Case

หน้าจอ SmartLamp ไม่ควรเรียก BLE Library โดยตรงทุกจุด เพราะจะทำให้ UI ผูกกับรายละเอียดการสแกน การเชื่อมต่อ Packet และ Permission มากเกินไป

โครงสร้างที่เหมาะสมคือให้ UI เรียก **Use Case** หรือ **Repository** แล้วชั้นดังกล่าวจึงติดต่อ BLE Transport อีกทอดหนึ่ง

```text
ผู้ใช้
  ↓
React Native UI
  ↓
Use Case / Repository
  ↓
BLE Transport และ Protocol Codec
  ↓
ESP32
```

การแยกชั้นเช่นนี้ช่วยให้แต่ละส่วนมีหน้าที่ชัด เปลี่ยน BLE Library ได้ง่ายขึ้น และใช้ Fake หรือ Mock Transport ใน Unit/Integration Test ได้โดยไม่ต้องมีอุปกรณ์จริงตลอดเวลา

> **หลักคิด:** UI ควรบอกว่า “ต้องการทำอะไร” เช่น ตั้งความสว่างเป็น 60% ส่วนชั้น Transport รับผิดชอบว่า “จะส่งอย่างไร” ผ่าน BLE

---

## 18. Golden Protocol Vectors: สัญญาร่วมระหว่างแอปและ Firmware

SmartLamp มีโค้ดสองฝั่งที่ใช้คนละภาษา: แอปใช้ TypeScript ส่วน Firmware ใช้ C++ แม้ทั้งสองฝั่งอ่านเอกสาร Protocol เดียวกัน ก็อาจตีความรูปแบบ Byte, ลำดับข้อมูล หรือ CRC ไม่ตรงกันได้

วิธีตรวจที่มีประสิทธิภาพคือสร้าง **Golden Protocol Vectors** หรือชุดตัวอย่างมาตรฐานที่ระบุ Input และผลลัพธ์ Byte ที่คาดหวังไว้อย่างแน่นอน จากนั้นให้ทั้ง TypeScript และ C++ ใช้ชุดข้อมูลเดียวกันทดสอบการ Encode และ Decode

```text
คำสั่งมาตรฐานเดียวกัน
        ↓
TypeScript encode ── ต้องได้ Byte ชุดเดียวกัน
C++ encode        ── ต้องได้ Byte ชุดเดียวกัน
TypeScript/C++ decode ── ต้องแปลกลับเป็นข้อมูลเดียวกัน
```

หากฝั่งหนึ่งผ่านแต่อีกฝั่งไม่ผ่าน แสดงว่าเกิด **Protocol Drift** หรือความเข้าใจข้อตกลงเริ่มต่างกัน การทดสอบด้วย Golden Vectors จึงมีความแม่นยำกว่าการดูเพียงว่าสีบน UI คล้ายกับสีของหลอดไฟ

---

## 19. เชื่อมความรู้ทั้งหมดเป็นภาพเดียว

เมื่อผู้ใช้เปิดแอป SmartLamp ผู้ใช้กำลังทำงานกับ Front-End ที่ประกอบด้วย Component หลายส่วน รูปแบบและการจัดวางต้อง Responsive และเข้าถึงได้ ข้อมูลใน UI ถูกจัดการด้วย Props และ State ขณะที่ TypeScript ช่วยตรวจโครงสร้างข้อมูล

เมื่อผู้ใช้กดปุ่มหรือเลื่อน Slider จะเกิด Event แล้ว Function หรือ Use Case ทำงาน คำสั่งถูกส่งผ่าน Repository ไปยัง BLE Transport จากนั้น ESP32 ประมวลผลและส่ง ACK หรือ State กลับมา UI ต้องถือค่าที่ยืนยันจาก ESP32 เป็นค่าจริงหลัก พร้อมใช้ Sequence ป้องกันคำสั่งซ้ำ Revision ป้องกันข้อมูลเก่า และ CRC ตรวจ Packet เสียหาย

นอกเหนือจากการทำงานของแอป ทีมยังต้องใช้ Git และ Repository เพื่อจัดการประวัติโค้ด เก็บ Secret อย่างปลอดภัย และเขียนทั้ง Unit Test กับ E2E Test การใช้ Golden Protocol Vectors ช่วยยืนยันว่าแอป TypeScript และ Firmware C++ ยังสื่อสารด้วยข้อตกลงเดียวกัน

ภาพรวมนี้คือรากฐานสำคัญก่อนเข้าสู่รายละเอียดของ HTML/CSS, JavaScript/TypeScript, React, API, Authentication, Testing และการพัฒนา SmartLamp จริง

---

## 20. สรุปสิ่งที่ควรอธิบายได้ก่อนเริ่มเรียน

หลังอ่านเอกสารนี้ ควรอธิบายด้วยภาษาของตนเองได้ว่า HTML, CSS และ JavaScript แบ่งหน้าที่กันอย่างไร รวมถึงรู้จัก Browser, URL, DOM, Event และแท็ก HTML พื้นฐาน

ควรแยกตัวแปร Boolean Array Function `const` และ `map()` ได้ เข้าใจเหตุผลที่ต้องมี asynchronous programming และเห็นประโยชน์ของ TypeScript

ควรอธิบายความสัมพันธ์ระหว่าง Component, Props และ State ใน React รวมถึงเข้าใจว่า API, JSON, HTTP `GET`, Status Code `404` และ CRUD เกี่ยวข้องกันอย่างไร

ควรแยก Authentication ออกจาก Authorization รู้ว่า Secret ไม่ควรอยู่ในโค้ดฝั่งผู้ใช้หรือ Git สาธารณะ และเข้าใจบทบาทของ Git, Repository, Unit Test และ E2E Test

สำหรับ SmartLamp ควรรู้จัก RGB, HEX, Bluetooth และ BLE เข้าใจว่าทำไม ESP32 เป็น Source of Truth เมื่อเชื่อมต่อแล้ว เหตุใด Slider ต้องจำกัดอัตราส่ง และสามารถแยกหน้าที่ของ Revision, Sequence และ CRC ได้

สุดท้าย ควรอธิบายได้ว่าการแยก UI ออกจาก BLE ผ่าน Repository/Use Case ช่วยให้ระบบดูแลและทดสอบง่ายขึ้นอย่างไร และเหตุใด TypeScript กับ C++ จึงควรทดสอบด้วย Golden Protocol Vectors ชุดเดียวกัน

หากยังมีคำศัพท์ใดอธิบายไม่ได้ ให้ย้อนกลับไปอ่านส่วนนั้นและลองยกตัวอย่างจากหน้าควบคุม SmartLamp ด้วยตนเอง การอธิบายเป็นเหตุและผลได้มีประโยชน์กว่าการจำคำตอบโดยไม่เข้าใจความเชื่อมโยง
